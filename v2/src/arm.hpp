#pragma once
#include <iostream>
// #include <pigpio.h>

class Arm{
public:
    Arm(){
        // if(gpioInitialise() < 0){
        //     std::cerr << "Couldn't initialise pigpio\n";
        // }
    }
};